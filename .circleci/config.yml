orbs:
  node: circleci/node@4.0.0

version: 2.1

jobs:
  build:
    docker:
      - image: 'cimg/base:stable'
    steps:
      - node/install:
          install-yarn: true
      - run: ls -la
      - run: cd ../
      - run: ls -la
      - run: yarn 
      - run: ls -la
      - run: yarn lint
      - run: yarn prettier
      - run: yarn build
 

workflows:
  node-tests:
    jobs:
      - build
      


