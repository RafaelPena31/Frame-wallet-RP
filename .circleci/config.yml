orbs:
  node: circleci/node@4.0.0

version: 2.1

jobs:
  build:
    docker:
      - image: 'cimg/base:stable'
    steps:
      - node/install:
          install-yarn: true
      - run: cd frame-wallet
      - run: yarn 
      - run: yarn lint
      - run: yarn prettier
      - run: yarn build
 

workflows:
  node-tests:
    jobs:
      - build
      


